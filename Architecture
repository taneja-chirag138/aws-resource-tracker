AWS Account
     │
     │ (AWS CLI)
     ▼
Shell Script (aws_resource_tracker.sh)
     │
     │ Collects AWS resource data
     ▼
Log File (aws_resource.log)
     │
     │ Cron Job runs every 5 minutes
     ▼
Mail Service (Postfix / sendmail)
     │
     ▼
Email Sent to Admin
